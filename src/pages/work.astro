---
import BaseLayout from '../layouts/BaseLayout.astro';
import PortfolioPost from '../components/PortfolioPost.astro'; 
const allPortfolio = Object.values(import.meta.glob('./portfolio/*.md', { eager: true }));
const pageTitle = "Work";
---

<BaseLayout pageTitle={pageTitle}>
		<div id="portfolio" class="content">
		<h1>{pageTitle}</h1>
        <p>This will be my work home gallery, to which everything else will link eventually (i hope!!)</p>
        <ul>
          {allPortfolio.map((portfolio: any) => <PortfolioPost url={portfolio.url} title={portfolio.frontmatter.title} />)}
        </ul>
		</div>
</BaseLayout>